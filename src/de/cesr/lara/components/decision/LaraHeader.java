package de.cesr.lara.components.decision;

/**
 * header row for matrix
 */
public interface LaraHeader {

	/**
	 * adds a cell to the header
	 * 
	 * @param title
	 */
	public void addCell(String title);

}
